<UserControl x:Class="ShopDrawing.Plugin.UI.ExportPdfPaletteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="300"
             Background="#2D2D30">
             
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style TargetType="Border" x:Key="SectionBorder">
            <Setter Property="BorderBrush" Value="#3E3E42" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Background" Value="#252526" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="0,4,0,10" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="4,0" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="24" />
            <Setter Property="Background" Value="#3F3F46" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Height" Value="32" />
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Margin" Value="0,10,0,0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C97EA" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#555555" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="XUẤT PDF" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#007ACC" />

            <!-- Print Settings -->
            <Border Grid.Row="1" Style="{StaticResource SectionBorder}">
                <StackPanel>
                    <TextBlock Text="Cài đặt Máy in (Plotter)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#007ACC" />
                    <TextBlock Text="Máy in:" />
                    <ComboBox x:Name="cmbPlotters" />

                    <TextBlock Text="Plot Style (CTB):" />
                    <ComboBox x:Name="cmbPlotStyles" />
                </StackPanel>
            </Border>

            <!-- Output Settings -->
            <Border Grid.Row="2" Style="{StaticResource SectionBorder}">
                <StackPanel>
                    <TextBlock Text="Cài đặt File đầu ra" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#007ACC" />
                    
                    <TextBlock Text="Thư mục xuất PDF:" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtOutputPath" Grid.Column="0" Margin="0,4,5,10" />
                        <Button x:Name="btnBrowse" Grid.Column="1" Content="📁" Width="30" Margin="0,4,0,10" Click="BtnBrowse_Click" ToolTip="Chọn thư mục" />
                    </Grid>

                    <TextBlock Text="Tên file PDF:" />
                    <TextBox x:Name="txtFileName" />
                    
                    <CheckBox x:Name="chkOpenPdf" Content="Mở PDF sau khi hoàn tất" IsChecked="True" Foreground="#E0E0E0" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <!-- Export Button -->
            <Button x:Name="btnExport" Grid.Row="3" Content="📄 Xuất PDF" Style="{StaticResource ActionButtonStyle}" Click="BtnExport_Click" />

            <!-- Status -->
            <TextBlock x:Name="txtStatus" Grid.Row="4" Margin="0,15,0,0" Text="Sẵn sàng." TextWrapping="Wrap" Foreground="#9ACD32" />
        </Grid>
    </ScrollViewer>
</UserControl>
